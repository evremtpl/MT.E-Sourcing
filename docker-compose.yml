version: '3.4'

services:
  sourcingdb:
     image: mongo


  mt.e-sourcing.products:
    image: ${DOCKER_REGISTRY-}emerveproduct
    build:
      context: .
      dockerfile: MT.E-Sourcing.Products/Dockerfile
